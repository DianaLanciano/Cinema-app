import express from 'express';

const router = express.Router();

// router.get('/', getAllShowtime); // Fetch all showtimes with optional filtering (e.g., by date, movie ID)

//  router.post("/create", createShowTime);

//  router.put("/update/:showTimeId", updateShowTime);

//  router.delete("delete/:showTimeId", deleteShowTime);
 
 export default router;